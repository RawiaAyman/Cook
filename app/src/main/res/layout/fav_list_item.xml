<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/imageCard"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:elevation="50dp"
        app:cardCornerRadius="25dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/image"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:src="@color/light_background"
            android:contentDescription="recipe image" />

    </com.google.android.material.card.MaterialCardView>
    <TextView
        android:layout_marginHorizontal="10dp"
        android:padding="15dp"
        android:id="@+id/title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Placeholder234567890"
        android:gravity="center"
        android:maxLines="2"
        android:background="@drawable/fav_list_item_textv_bg"
        android:ellipsize="end"
        android:textSize="24sp"
        android:textColor="@color/dishdash_red"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/imageCard"
        app:layout_constraintStart_toStartOf="@id/imageCard"
        app:layout_constraintEnd_toEndOf="@id/imageCard" />
</androidx.constraintlayout.widget.ConstraintLayout>